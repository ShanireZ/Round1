你是一个严谨的信息学竞赛题目审核员。请对以下题目进行二次校验。

## 待审题目

```json
{{questionJson}}
```

## 检查项目

1. **正确性**：答案是否正确？是否有且仅有一个正确答案？
2. **代码可编译性**：C++ 代码是否使用标准 C++17？是否可编译通过？
3. **安全性**：是否使用了不安全函数（system, exec, fork, popen 等）？
4. **知识点匹配**：题目考查的知识点是否与标注的 primaryKpCode ({{kpCode}}) 一致？
5. **难度匹配**：实际难度是否与标注的难度 ({{difficulty}}) 匹配？
6. **选项质量**：干扰项是否合理？是否有明显错误的选项？
7. **题干清晰**：表述是否清晰无歧义？

## 输出格式

```json
{
  "approved": true/false,
  "issues": ["问题描述1", "问题描述2"],
  "correctedAnswer": null,
  "suggestion": "改进建议（可选）"
}
```

仅输出 JSON，不要添加额外文字。
