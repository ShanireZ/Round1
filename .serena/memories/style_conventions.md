Prefer small targeted edits; use apply_patch for manual file edits. Respect existing uncommitted changes. Offline formal assets must keep runId-based naming and auditability. Temporary drafts stay under artifacts/tmp, formal question bundles under papers/<year>/<runId>/question-bundles. For content work, validate before import; do not write directly to production runtime paths or bypass import workflow.